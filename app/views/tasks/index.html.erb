<h1>Tasks#index</h1>

<% @tasks.each do |task| %>
  <h2>Title <%= task.id %>: <%= link_to task.title, task_path(task.id)%></h2>
  <p>Description: <%= task.description %></p>
  <p>Done: <%= task.done %></p>
<% end %>

<%= link_to "New task", new_task_path %>
